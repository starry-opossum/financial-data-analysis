# 企业融资需求测算（Python 示例项目）

基于历史收入数据和资产负债表，使用时间序列预测（霍尔特‑温特模型）预测未来收入增长率，并通过销售百分比法计算企业的外部融资需求，最后生成可视化面积图。

> 本项目数据为**模拟示例数据**，仅用于展示技术流程。

## 项目结构
├── 01SQL语句获取经营数据.py # 从 MySQL 数据库拉取收入数据（需配置 .env）
├── 02Python计算增长率.py # 收入预测与增长率计算
├── 03Python计算资产负债表中销售敏感项目.py # 识别与收入高度相关的敏感项目
├── 04Python融资需求可视化分析.py # 计算融资需求并生成 HTML 图表
├── 收入合计.xlsx # 示例：日收入明细（模拟）
├── 案例资源资产负债表.xlsx # 示例：历年资产负债表（模拟）
├── .env.example # 环境变量示例文件
├── requirements.txt # Python 依赖库
└── README.md

## 功能说明

1. **收入数据预处理**：将日收入重采样为季度/年度合计。
2. **收入预测**：采用霍尔特‑温特加法模型（趋势 + 季节）预测未来 4 个季度收入，计算年度增长率。
3. **敏感项目识别**：计算资产负债表各科目与收入的相关系数，|r|>0.8 视为敏感项目。
4. **融资需求计算**：
   - 融资总需求 = 敏感资产变动总额 - 敏感负债变动总额
   - 净利润 = 预测期收入 × 5%（净利率假设，可调）
   - 外部融资需求 = 融资总需求 - 净利润
5. **可视化**：生成资产总额、负债总额、营业收入随年份变化的面积图（交互式 HTML）。

## 依赖安装

```bash
pip install pandas numpy statsmodels pyecharts pymysql openpyxl python-dotenv
